---
title: "旅行预算计算器"
description: "出国旅行预算规划和费用计算工具，支持多币种"
---

# 💰 旅行预算计算器

<script setup>
import { onMounted } from 'vue'

onMounted(() => {
  const symbols = { CNY:'¥', USD:'$', EUR:'€', JPY:'¥', THB:'฿', KRW:'₩', GBP:'£', AUD:'A$' }

  function calculate() {
    const days = parseInt(document.getElementById('days').value) || 1
    const people = parseInt(document.getElementById('people').value) || 1
    const curr = document.getElementById('currency').value
    const sym = symbols[curr] || '¥'
    const flights = parseFloat(document.getElementById('flights').value) || 0
    const insurance = parseFloat(document.getElementById('insurance').value) || 0
    const visa = parseFloat(document.getElementById('visa').value) || 0

    let dailyTotal = 0
    const rows = document.querySelectorAll('.daily')
    rows.forEach(input => { dailyTotal += parseFloat(input.value) || 0 })

    const total = (dailyTotal * days * people) + flights + insurance + visa

    function format(n) {
      const s = curr === 'JPY' || curr === 'KRW' ? Math.round(n).toString() : Math.round(n).toLocaleString('zh-CN')
      return sym + ' ' + s
    }

    document.querySelectorAll('.total-cell').forEach((cell, i) => {
      cell.textContent = format((parseFloat(rows[i].value) || 0) * days * people)
    })

    document.getElementById('totalAmount').textContent = format(total)
    document.getElementById('perPerson').textContent = '人均 ' + format(total / people)
    document.getElementById('perDay').textContent = '日均 ' + format(total / days / people)
  }

  calculate()
  document.querySelectorAll('input, select').forEach(el => el.addEventListener('input', calculate))
})
</script>

<div class="bc">
  <div class="bc-top">
    <div class="bc-summary">
      <div class="bc-summary-label">旅行总预算</div>
      <div class="bc-summary-amount" id="totalAmount">¥ 7,530</div>
      <div class="bc-summary-meta">
        <span>👤 <strong id="perPerson">人均 ¥ 7,530</strong></span>
        <span>📅 <strong id="perDay">日均 ¥ 1,076</strong></span>
      </div>
    </div>
  </div>

  <div class="bc-section">
    <div class="bc-section-title">📋 行程基础</div>
    <div class="bc-grid-3">
      <div class="bc-field">
        <label>出行天数</label>
        <input type="number" id="days" value="7" min="1" max="90">
      </div>
      <div class="bc-field">
        <label>出行人数</label>
        <input type="number" id="people" value="1" min="1" max="20">
      </div>
      <div class="bc-field">
        <label>货币单位</label>
        <select id="currency">
          <option value="CNY">🇨🇳 人民币 ¥</option>
          <option value="USD">🇺🇸 美元 $</option>
          <option value="EUR">🇪🇺 欧元 €</option>
          <option value="JPY">🇯🇵 日元 ¥</option>
          <option value="THB">🇹🇭 泰铢 ฿</option>
          <option value="KRW">🇰🇷 韩元 ₩</option>
          <option value="GBP">🇬🇧 英镑 £</option>
          <option value="AUD">🇦🇺 澳元 A$</option>
        </select>
      </div>
    </div>
  </div>

  <div class="bc-section">
    <div class="bc-section-title">🏠 每日花费</div>
    <div class="bc-cost-grid">
      <div class="bc-cost-item">
        <span class="bc-cost-label">🏨 住宿</span>
        <input type="number" class="daily" value="300">
        <span class="bc-cost-total total-cell">—</span>
      </div>
      <div class="bc-cost-item">
        <span class="bc-cost-label">🍜 餐饮</span>
        <input type="number" class="daily" value="200">
        <span class="bc-cost-total total-cell">—</span>
      </div>
      <div class="bc-cost-item">
        <span class="bc-cost-label">🚇 当地交通</span>
        <input type="number" class="daily" value="80">
        <span class="bc-cost-total total-cell">—</span>
      </div>
      <div class="bc-cost-item">
        <span class="bc-cost-label">🎫 景点门票</span>
        <input type="number" class="daily" value="100">
        <span class="bc-cost-total total-cell">—</span>
      </div>
      <div class="bc-cost-item">
        <span class="bc-cost-label">🛍️ 购物纪念品</span>
        <input type="number" class="daily" value="100">
        <span class="bc-cost-total total-cell">—</span>
      </div>
      <div class="bc-cost-item">
        <span class="bc-cost-label">📦 其他杂费</span>
        <input type="number" class="daily" value="50">
        <span class="bc-cost-total total-cell">—</span>
      </div>
    </div>
  </div>

  <div class="bc-section">
    <div class="bc-section-title">✈️ 一次性费用</div>
    <div class="bc-grid-3">
      <div class="bc-field">
        <label>往返机票</label>
        <input type="number" id="flights" value="2000" min="0">
      </div>
      <div class="bc-field">
        <label>旅行保险</label>
        <input type="number" id="insurance" value="200" min="0">
      </div>
      <div class="bc-field">
        <label>签证费用</label>
        <input type="number" id="visa" value="0" min="0">
      </div>
    </div>
  </div>
</div>

<style>
.bc { max-width: 680px; margin: 0 auto; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; }

/* Top Summary Card */
.bc-top { margin-bottom: 24px; }
.bc-summary {
  background: linear-gradient(135deg, #1a7a3a 0%, #2d9f4e 100%);
  color: #fff; border-radius: 16px; padding: 28px; text-align: center;
  box-shadow: 0 4px 20px rgba(26, 122, 58, 0.25);
}
.bc-summary-label { font-size: 14px; opacity: 0.85; margin-bottom: 4px; }
.bc-summary-amount { font-size: 40px; font-weight: 800; letter-spacing: 2px; margin: 6px 0; }
.bc-summary-meta { display: flex; justify-content: center; gap: 24px; margin-top: 8px; font-size: 14px; opacity: 0; }
.bc-summary-meta { opacity: 0.9; }
.bc-summary-meta span { background: rgba(255,255,255,0.15); padding: 4px 16px; border-radius: 20px; }

/* Sections */
.bc-section { background: #fff; border: 1px solid #e8ecf0; border-radius: 12px; padding: 20px; margin-bottom: 16px; }
.bc-section-title { font-size: 15px; font-weight: 700; color: #333; margin-bottom: 14px; }

/* Grid Layouts */
.bc-grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
.bc-field label { display: block; font-size: 13px; font-weight: 600; color: #555; margin-bottom: 6px; }
.bc-field input, .bc-field select {
  width: 100%; padding: 10px 12px; border: 1px solid #d0d5dd; border-radius: 8px;
  font-size: 15px; background: #fff; box-sizing: border-box; transition: border-color 0.2s;
}
.bc-field input:focus, .bc-field select:focus {
  outline: none; border-color: #2d9f4e; box-shadow: 0 0 0 3px rgba(45, 159, 78, 0.12);
}

/* Daily Cost Grid */
.bc-cost-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; }
.bc-cost-item {
  display: flex; align-items: center; gap: 8px;
  padding: 10px 12px; background: #f8f9fb; border-radius: 8px;
}
.bc-cost-label { flex: 0 0 auto; font-size: 13px; font-weight: 600; color: #444; min-width: 80px; }
.bc-cost-item input {
  flex: 1; min-width: 0; padding: 6px 10px; border: 1px solid #ddd;
  border-radius: 6px; font-size: 14px; text-align: right; box-sizing: border-box;
}
.bc-cost-item input:focus { outline: none; border-color: #2d9f4e; }
.bc-cost-total {
  flex: 0 0 70px; font-size: 13px; font-weight: 700; color: #1a7a3a; text-align: right;
}

@media (max-width: 600px) {
  .bc-grid-3 { grid-template-columns: 1fr; }
  .bc-cost-grid { grid-template-columns: 1fr; }
  .bc-summary-amount { font-size: 30px; }
}
</style>
